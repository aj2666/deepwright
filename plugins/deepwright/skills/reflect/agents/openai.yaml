interface:
  display_name: "Reflect"
  short_description: "Turn task lessons into approved durable improvements"
  default_prompt: "Use $reflect to propose durable improvements from the lessons in this task."
policy:
  allow_implicit_invocation: false
