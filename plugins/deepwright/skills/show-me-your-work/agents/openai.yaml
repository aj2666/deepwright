interface:
  display_name: "Show Me Your Work"
  short_description: "Keep an evidence-backed decision trail for a run"
  default_prompt: "Use $show-me-your-work to keep an evidence-backed decision trail for this run."
policy:
  allow_implicit_invocation: false
