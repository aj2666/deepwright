interface:
  display_name: "Interrogate"
  short_description: "Run independent adversarial reviews of a design or diff"
  default_prompt: "Use $interrogate to run a read-only adversarial review of this design or diff."
policy:
  allow_implicit_invocation: false
