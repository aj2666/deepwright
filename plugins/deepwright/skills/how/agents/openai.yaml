interface:
  display_name: "How"
  short_description: "Explain code behavior, ownership, and data flow"
  default_prompt: "Use $how to trace how this subsystem works from entry point to effect."
policy:
  allow_implicit_invocation: false
