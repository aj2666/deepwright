interface:
  display_name: "Maintain Verification Skill"
  short_description: "Keep a project's verification skill accurate"
  default_prompt: "Use $maintain-verification-skill to audit this project's feature map and verification harness."
policy:
  allow_implicit_invocation: false
