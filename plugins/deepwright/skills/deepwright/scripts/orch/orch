#!/bin/sh
set -eu

scripts_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
bundle="$scripts_dir/dist/orch.mjs"

if [ ! -f "$bundle" ]; then
  echo "deepwright-orch: bundled CLI is missing; run 'npm run build' in $scripts_dir" >&2
  exit 78
fi

exec node "$bundle" "$@"
