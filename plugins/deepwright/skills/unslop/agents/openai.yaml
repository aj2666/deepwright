interface:
  display_name: "Unslop"
  short_description: "Rewrite AI-sounding prose without changing facts or voice"
  default_prompt: "Use $unslop to remove generic AI patterns while preserving facts, uncertainty, and the writer's voice."
policy:
  allow_implicit_invocation: false
