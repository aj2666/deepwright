interface:
  display_name: "Unslop"
  short_description: "Remove generic AI phrasing and restore a human voice"
  default_prompt: "Use $unslop to remove generic AI phrasing and restore a specific human voice."
policy:
  allow_implicit_invocation: false
