# Save and cancel

Approved behavior: saveDocument returns ok only when storage succeeds; a storage failure must reach the caller. Cancellation of a preview is expected and returns cancelled. Review the implementation and assertion quality. No execution results are supplied.
