# Search cache keys

The search and cache layers need the same key normalization: trim surrounding whitespace and lowercase the text. Preserve internal whitespace. A design decision is requested before any changes or installations.
