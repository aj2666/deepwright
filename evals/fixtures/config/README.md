# Greeting example

`greeting(name)` returns a greeting after trimming the supplied name. Run
`node --test greeting.test.mjs` for the local tests. The project has no package
or network dependencies.

Optional Deepwright project preferences are kept in `.codex/deepwright.toml`.
