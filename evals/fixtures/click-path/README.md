# Item picker

Choosing an item should close the picker and keep the chosen item visible. Cancelling should close it and clear the temporary choice. The picker module is a local state model for those UI handlers.
