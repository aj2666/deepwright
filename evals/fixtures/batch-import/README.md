# Import service

`importRecords` divides records into batches and sends them to a provider. The
current provider adapter is a local stub. Design and operational records are in
`records/`; these are the available historical materials for this checkout.
